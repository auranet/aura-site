
<%=
  Meteor::MeteorRenderer.new(:spec => @spec,
                             :controller => controller,
                             :frontend => "meteor",
                             :params => params,
                             :id => User.all[0].id).render
%>
